export interface Transacoes {
    idTransacao?: string;
    nomeTransacao: string;
    tipoCompra: string;
    status: string;
    formaPagamento:string;
    valor: number;
    // statusTransacao: boolean;
}